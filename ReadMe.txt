Every file in this folder is in Git repository, so feel free to update. You can always revert back using 

  git checkout -f [file_or_filder]


1. Design UI using Omini Graffle



2. Rails app (skeletion has already created)
   To start the application
	 
	 $ cd /Users/zhimin/work/courtney/hotelwise/hotelwise-rails
	 $ rails s
	 
	 then open http://localhost:3000 in browser
	 

3. Edit code using TextMate and open a new terminal for command 

   for example, generate a new model and migrate database
	 
	 $ rails g model User
	 $ rake db:migrate
	 

